{% schema %}
{
  "name": "Custom Testimonial Slider",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Esto es lo que dicen nuestros cliente"
    },
    {
      "type": "color",
      "id": "bg_color",
      "label": "Background Color",
      "default": "#fdf8f3"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Top",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Padding Bottom",
      "default": 80
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "CTA Button Label",
      "default": "Quiero empezar"
    },
    {
      "type": "url",
      "id": "cta_link",
      "label": "CTA Button Link"
    }
  ],
  "blocks": [
    {
      "type": "testimonial",
      "name": "Testimonial",
      "settings": [
        {
          "type": "image_picker",
          "id": "user_image",
          "label": "Client Image"
        },
        {
          "type": "text",
          "id": "client_name",
          "label": "Client Name/Location",
          "default": "Michelle – California (USA)"
        },
        {
          "type": "textarea",
          "id": "testimonial_text",
          "label": "Testimonial Text",
          "default": "I'm Michelle, from California..."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Testimonial Slider"
    }
  ]
}
{% endschema %}

<style>
  @import url('https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,300;0,400;0,600;0,700;1,400&display=swap');

  .testimonial-section-{{ section.id }} { 
    padding-top: {{ section.settings.padding_top }}px; 
    padding-bottom: {{ section.settings.padding_bottom }}px; 
    background-color: {{ section.settings.bg_color }};
    font-family: 'Poppins', sans-serif !important;
  }
  
  .testimonial-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 40px;
    position: relative;
    box-sizing: border-box;
  }
  
  .testimonial-title {
    text-align: center;
    font-family: 'Poppins', sans-serif !important;
    font-size: clamp(24px, 4vw, 32px);
    font-weight: 800;
    margin-bottom: 80px;
    color: #000;
    letter-spacing: -0.5px;
  }

  .testimonial-slider-wrapper { 
    position: relative;
    display: flex;
    align-items: center;
  }

  .testimonial-slide { 
    display: none; 
    width: 100%; 
  }

  .testimonial-slide.active { 
    display: flex; 
    justify-content: center;
    align-items: center;
    gap: 60px; /* Gap between image and text */
    animation: fadeIn 0.5s ease-in-out;
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  .testimonial-image-wrapper {
    flex: 0 0 300px;
    max-width: 100%;
    height: 300px;
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0,0,0,0.05);
  }

  .main-img { 
    width: 100%; 
    height: 100%; 
    object-fit: cover; 
  }

  .testimonial-text-wrapper { 
    flex: 0 1 550px; /* Fixed width for text area */
  }

  .client-name {
    color: #ff8c42;
    font-size: clamp(18px, 3vw, 22px);
    font-weight: 700;
    font-family: 'Poppins', sans-serif !important;
    margin-bottom: 16px;
  }

  .testimonial-body { 
    font-size: 14.5px; 
    font-family: 'Poppins', sans-serif !important;
    line-height: 1.7; 
    color: #555; 
    font-style: italic;
    font-weight: 400;
  }

  /* Navigation Buttons - Positioned like the image */
  .nav-btn { 
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    border: none; 
    border-radius: 50%; 
    width: 50px; 
    height: 50px; 
    cursor: pointer; 
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    transition: 0.3s;
    font-size: 20px;
    color: white;
  }

  .prev-btn { 
    left: -20px; 
    background: #000; 
  }

  .next-btn { 
    right: -20px; 
    background: #ff8c42; 
  }

  .nav-btn:hover { opacity: 0.8; transform: translateY(-50%) scale(1.05); }

  @media (max-width: 1024px) {
    .testimonial-slide.active { gap: 30px; }
    .testimonial-image-wrapper { flex: 0 0 260px; height: 260px; }
    .testimonial-container { padding: 0 24px; }
  }

  @media (max-width: 768px) {
    .testimonial-slide.active { flex-direction: column; text-align: center; gap: 24px; }
    .testimonial-image-wrapper { flex: 0 0 auto; width: 240px; height: 240px; margin: 0 auto; }
    .testimonial-text-wrapper { flex: 1 1 auto; max-width: 100%; }
    .nav-btn { position: relative; top: 0; transform: none; margin: 16px 8px; display: inline-flex; }
    .testimonial-slider-wrapper { display: block; text-align: center; }
    .testimonial-title { margin-bottom: 32px; }
    .testimonial-container { padding: 0 16px; }
    .prev-btn { left: 0; }
    .next-btn { right: 0; }
  }

  @media (max-width: 480px) {
    .testimonial-image-wrapper { width: 200px; height: 200px; }
    .testimonial-body { font-size: 13px; }
    .nav-btn { width: 42px; height: 42px; font-size: 16px; }
  }

  .testimonial-cta-wrap {
    text-align: center;
    margin-top: 50px;
  }

  .testimonial-cta-btn {
    display: inline-block;
    background: #ff914d;
    color: #fff !important;
    padding: 14px 40px;
    border-radius: 50px;
    font-family: 'Poppins', sans-serif;
    font-size: 15px;
    font-weight: 700;
    text-decoration: none;
    transition: background 0.3s, transform 0.2s;
  }

  .testimonial-cta-btn:hover {
    background: #000;
    color: #ff914d !important;
    transform: translateY(-2px);
  }
</style>

<section class="testimonial-section-{{ section.id }}">
  <div class="testimonial-container">
    {% if section.settings.heading != blank %}
      <h2 class="testimonial-title">{{ section.settings.heading }}</h2>
    {% endif %}

    <div class="testimonial-slider-wrapper">
      <button class="nav-btn prev-btn" onclick="moveSlide(-1)">❮</button>

      {% for block in section.blocks %}
        <div class="testimonial-slide {% if forloop.first %}active{% endif %}" data-index="{{ forloop.index0 }}" {{ block.shopify_attributes }}>
            
            <div class="testimonial-image-wrapper">
              {% if block.settings.user_image %}
                <img src="{{ block.settings.user_image | img_url: '800x800', crop: 'center' }}" alt="{{ block.settings.client_name }}" class="main-img">
              {% else %}
                {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
              {% endif %}
            </div>

            <div class="testimonial-text-wrapper">
              <h3 class="client-name">{{ block.settings.client_name }}</h3>
              <div class="testimonial-body">
                {{ block.settings.testimonial_text }}
              </div>
            </div>

        </div>
      {% endfor %}

      <button class="nav-btn next-btn" onclick="moveSlide(1)">❯</button>
    </div>

    {% if section.settings.cta_label != blank %}
      <div class="testimonial-cta-wrap">
        <a href="{{ section.settings.cta_link }}" class="testimonial-cta-btn">{{ section.settings.cta_label }}</a>
      </div>
    {% endif %}
  </div>
</section>

<script>
let currentIdx = 0;
function moveSlide(direction) {
  const container = document.querySelector('.testimonial-section-{{ section.id }}');
  const slides = container.querySelectorAll('.testimonial-slide');
  
  slides[currentIdx].classList.remove('active');
  
  currentIdx += direction;
  
  if (currentIdx >= slides.length) {
    currentIdx = 0;
  } else if (currentIdx < 0) {
    currentIdx = slides.length - 1;
  }
  
  slides[currentIdx].classList.add('active');
}
</script>